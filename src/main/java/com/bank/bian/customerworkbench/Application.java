package com.bank.bian.customerworkbench;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Customer Workbench
 *
 * Business Area:      Sales and Service
 * Business Domain:    Channels
 * Functional Pattern: Operate
 * Asset Type:         Servicing Session
 * Control Record:     Servicing Session Operating Session
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Customer Workbench — BIAN Semantic API")
                .description("Service Domain 'Customer Workbench' (Operate pattern) "
                        + "operating on the 'Servicing Session Operating Session' control record.")
                .version("0.1.0"));
    }
}
